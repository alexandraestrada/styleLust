<h1>Category</h1>
 <div class="row">
 		<div class="small-12 columns deckcontainer">
	    <div class="large-12 small-12 columns" id="itemdeck">
	      <!-- <img src="http://placehold.it/450x450&text=[img]"> -->
	      <% url = @items.all[@index].click_url %>
        <% image = @items.all[@index].photo %>
        <%= link_to image_tag(image) , url %><br /> 
	      <br><br>
	    </div>
	  </div>
		<%= @items.first.name %>
        <%= @items.first.price %>
    <br><br>
 	 	<div class="large-12 small-12 columns" id="likebar">
 	 		<%= link_to "Dislike", new_item_path(cat: params[:cat], price: params[:price]), class: "button", method: 'create' %>
 	 		<%= link_to "Buy Now", url, class: "button" %>
 			<%= link_to "Love", new_item_path(cat: params[:cat], price: params[:price]), class: "button", method: 'create' %>
    </div>
  </div>